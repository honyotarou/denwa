[modules]
autoload=yes

; 5060 は res_pjsip のみ（chan_sip が先に REGISTER を受けると extensions.conf の secret と不一致になる）
noload=chan_sip.so

; 不要モジュールはここで noload してもいいが、MVP では autoload に任せる。
; PJSIP / dialplan / MixMonitor (録音) / func_curl / app_system を使う。
