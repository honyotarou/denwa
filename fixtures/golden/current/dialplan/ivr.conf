; AUTO-GENERATED by OpenPBX.
; updated_at: 2026-05-19T00:00:00.000Z

[internal]
exten => 5000,1,NoOp(IVR 5000 entry)
 same => n,Goto(ivr-5000,s,1)

[ivr-5000]
exten => s,1,Answer()
 same => n,Wait(1)
 same => n,Playback(welcome)
 same => n,Set(IVR_TRIES=0)
 same => n,Goto(menu,1)

exten => menu,1,Set(IVR_TRIES=$[${IVR_TRIES} + 1])
 same => n,GotoIf($[${IVR_TRIES} > 3]?give-up)
 same => n,Background(menu)
 same => n,WaitExten(6)
 same => n(give-up),Playback(bye)
 same => n,Hangup()

exten => 1,1,NoOp(IVR 5000 option 1)
 same => n,Goto(internal,1001,1)

exten => 2,1,NoOp(IVR 5000 option 2)
 same => n,Goto(ringgroups,6000,1)

exten => t,1,Goto(menu,1)
exten => i,1,Goto(menu,1)
