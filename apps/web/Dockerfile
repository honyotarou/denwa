# Phase 2.5 stub — real Next.js image is wired in §6–7.
FROM node:22-alpine
WORKDIR /app
RUN echo '{"name":"command-room-web-stub"}' > package.json
CMD ["node", "-e", "console.log('command-room-web stub'); setInterval(() => {}, 60000)"]
